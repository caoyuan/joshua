java -Xmx10g -cp ./bin joshua.pro.MIRA mira.config

#Note: to use Meteor as metric, add the following path(where the Meteor can find its resources) to the classpath:
#java -cp ./bin:./joshua/zmert/meteor/resources joshua.pro.MIRA mira.config

